import 'package:flutter/material.dart';

class MYAccounts extends StatelessWidget {
  const MYAccounts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Accounts1'),
      )
      
    );
  }
}